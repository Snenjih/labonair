// TerminalStack is kept for export compatibility but is no longer used.
// WorkspacePane handles all terminal rendering in the new split-pane architecture.
export function TerminalStack() {
  return null;
}
